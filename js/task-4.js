const countTotalSalary = function(employees) {
    let total = 0;
    for(totalSalary of employees){
        total +=totalSalary;
        return total;
    }
    
  };
  
  
  console.log(countTotalSalary({}));